# nodejs-hw

Express + MongoDB homework project.

## Scripts

- `npm install`
- `npm run dev`
- `npm start`

## Environment variables

Create `.env` with:

```env
PORT=3000
MONGO_URL=your_mongodb_connection_string
```
